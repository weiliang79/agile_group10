language: php
sudo: required
dist: trusty

php:
  - 8.1.5

os: linux

before script:
  - composer install
  - cp .env.example .env
  - php artisan key:generate
  - php artisan jwt:secret

script:
  - vendor/bin/phpunit